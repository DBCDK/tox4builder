#!/usr/bin/env python

from distutils.core import setup

setup(
  name = 'jjbuilder',
  version = 'dev',
  describtion = 'Creating, uploading, and delete of jobs to a Jenkins CI server based on scripts which can be invoked from commandline.',
  packages = ['jjbuilder']
  )
